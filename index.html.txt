<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>DJ Strona</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- React -->
  <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
  <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>

  <!-- Babel (żeby JSX działał) -->
  <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
</head>

<body class="bg-black text-white">

<div id="root"></div>

<script type="text/babel">

function App() {

  const [form, setForm] = React.useState({
    name: "",
    email: "",
    event: "",
    date: "",
    message: ""
  });

  function handleChange(e) {
    setForm({ ...form, [e.target.name]: e.target.value });
  }

  function handleSubmit(e) {
    e.preventDefault();
    alert("🔥 Zapytanie wysłane! Odezwę się!");
  }

  return (
    <div>

      {/* HERO */}
      <section className="text-center py-20">
        <h1 className="text-5xl font-bold text-orange-500">
          DJ NA IMPREZY 🔥
        </h1>
        <p className="text-gray-300 mt-4">
          Wesela • 18-stki • Eventy • Kluby
        </p>
      </section>

      {/* O MNIE */}
      <section className="max-w-2xl mx-auto p-6">
        <h2 className="text-orange-500 text-2xl mb-2">O mnie</h2>
        <p className="text-gray-300">
          Gram imprezy, które ludzie pamiętają latami. Dopasowuję muzykę do klimatu.
        </p>
      </section>

      {/* FORMULARZ */}
      <section className="max-w-2xl mx-auto p-6">
        <h2 className="text-orange-500 text-2xl mb-4">
          Rezerwacja 📅
        </h2>

        <form onSubmit={handleSubmit} className="space-y-3">

          <input
            name="name"
            placeholder="Imię"
            onChange={handleChange}
            className="w-full p-2 bg-gray-900"
            required
          />

          <input
            name="email"
            placeholder="Email"
            onChange={handleChange}
            className="w-full p-2 bg-gray-900"
            required
          />

          <input
            name="event"
            placeholder="Rodzaj imprezy"
            onChange={handleChange}
            className="w-full p-2 bg-gray-900"
            required
          />

          <input
            type="date"
            name="date"
            onChange={handleChange}
            className="w-full p-2 bg-gray-900"
            required
          />

          <textarea
            name="message"
            placeholder="Dodatkowe info"
            onChange={handleChange}
            className="w-full p-2 bg-gray-900"
          />

          <button className="w-full bg-orange-500 p-2 font-bold">
            Wyślij 🚀
          </button>

        </form>
      </section>

    </div>
  );
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

</script>

</body>
</html>